# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts and Docker entrypoints MUST use LF — Linux refuses to exec
# a script whose shebang line ends in CR ("no such file or directory").
*.sh        text eol=lf
entrypoint  text eol=lf
Dockerfile  text eol=lf
